name: Run next phase

# Reusable workflow. Consumer repos call this from their own
# .github/workflows/orchestrator.yml — see examples/ for the template.
#
# Trust boundary: this workflow runs on the CONSUMER's runner with the
# CONSUMER's GITHUB_TOKEN. It reads .session-orchestrator/config.yml from
# the consumer repo, scans for the next ready phase, fires the headless
# Claude Code session, and persists markers + digest comment back to the
# consumer repo (default branch).
#
# Inputs are intentionally NOT taken from PR titles / branch names / commit
# messages — the only payload the orchestrator passes to Claude is the
# CONSUMER-authored kickoff doc at docs/handoffs/<pattern>. That removes the
# obvious prompt-injection surface from event-triggered runs.

on:
  workflow_call:
    inputs:
      orchestrator-ref:
        description: |
          Git ref of rsm0508/session-orchestrator to install — tag preferred
          (e.g. "v0.1.0"), branch ("main") or SHA also valid. Pin to a tag in
          production so a midnight push doesn't change behavior under you.
        type: string
        required: false
        default: 'main'
      dry-run:
        description: |
          When true, the orchestrator runs `next` then exits without firing.
          Useful for first-time wiring (verifies checkout, install, config,
          kill-switch checks) without burning API credit. Default false.
        type: boolean
        required: false
        default: false
      timeout-minutes:
        description: 'Wall-clock job timeout. Defaults to 30 minutes.'
        type: number
        required: false
        default: 30
    secrets:
      ANTHROPIC_API_KEY:
        required: true
      SESSION_ORCHESTRATOR_PAT:
        required: false # required only if rsm0508/session-orchestrator is private
      LINEAR_API_KEY:
        required: false # reserved for the kill-switch Linear-label seam (Day 3)
      SLACK_WEBHOOK_URL:
        required: false

permissions:
  contents: write # marker commits on the consumer's default branch
  issues: write # digest comment on the configured tracking issue
  pull-requests: write # the session itself opens PRs; granted at workflow level so the GITHUB_TOKEN inherits it

# Never cancel a fire in flight — the marker is already on disk and a
# cancellation would leave .started without .failed (the wrapper's pre-fire
# marker write is the duplicate-fire guard; canceling makes the phase look
# in-flight forever).
concurrency:
  group: session-orchestrator-${{ github.repository }}
  cancel-in-progress: false

jobs:
  fire:
    runs-on: ubuntu-latest
    timeout-minutes: ${{ fromJSON(inputs.timeout-minutes) }}
    steps:
      # TODO(supply-chain): pin actions/* by SHA before production. Using
      # major tags during Day 2 development for readability. Dependabot or
      # `gh api repos/actions/checkout/git/refs/tags/v5` can resolve.
      - name: Checkout consumer repo (default branch, regardless of trigger)
        # Codex workflow-R1 [P1] fix: actions/checkout's default ref varies by
        # trigger (PR base for pull_request, default branch for schedule).
        # That divergence means a cron run wouldn't see markers written by a
        # PR-triggered run on `feat/X`, and would re-fire phases. Force the
        # default branch on every trigger so all runs share one marker view.
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.repository.default_branch }}
          fetch-depth: 1
          # GITHUB_TOKEN scoped to the consumer repo — sufficient for
          # marker commits + digest issue comments + push back to default.
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node 22
        uses: actions/setup-node@v5
        with:
          node-version: '22'

      - name: Install Claude Code CLI
        run: npm install -g @anthropic-ai/claude-code

      - name: Install session-orchestrator CLI
        env:
          # Embedded into the git clone URL only when the PAT secret is set
          # (private rsm0508/session-orchestrator case). Never echoed.
          SESSION_ORCHESTRATOR_PAT: ${{ secrets.SESSION_ORCHESTRATOR_PAT }}
          ORCH_REF: ${{ inputs.orchestrator-ref }}
        run: |
          set -e
          # `npm install -g <git-url>` on npm 10.x silently symlinks the
          # globally-installed package to its cache's temp clone dir, which is
          # then cleaned up — leaving a dangling symlink and "command not
          # found" at runtime (reproduced locally on node:22 image, both with
          # github: shortcut and explicit git+file:// URLs). Fetch the ref
          # explicitly, pack, then install from the tarball: same internal
          # flow npm intends, but bypasses the broken git-URL hand-off.
          # `git init + git fetch <ref>` (not `git clone --branch <ref>`)
          # is required because the workflow input contract supports SHAs
          # in addition to branch/tag names, and --branch rejects raw SHAs.
          # Day-3 cleanup: switch to a published-tarball install once we
          # tag a release.
          TMPDIR=$(mktemp -d)
          SO_DIR="$TMPDIR/so"
          mkdir "$SO_DIR"
          cd "$SO_DIR"
          git init -q -b main
          if [ -n "$SESSION_ORCHESTRATOR_PAT" ]; then
            git remote add origin "https://${SESSION_ORCHESTRATOR_PAT}@github.com/rsm0508/session-orchestrator.git"
            git fetch --depth 1 origin "$ORCH_REF" > /dev/null 2>&1
          else
            git remote add origin https://github.com/rsm0508/session-orchestrator.git
            git fetch --depth 1 origin "$ORCH_REF"
          fi
          git checkout -q FETCH_HEAD
          TARBALL=$(npm pack --silent)
          npm install -g "$SO_DIR/$TARBALL"
          cd - >/dev/null
          session-orchestrator --version
          claude --version

      - name: Resolve next ready phase
        id: next
        run: |
          set -e
          OUTPUT=$(session-orchestrator next --json)
          echo "$OUTPUT"
          KIND=$(echo "$OUTPUT" | jq -r '.kind')
          PHASE=$(echo "$OUTPUT" | jq -r '.phase // empty')
          TRACKING_ISSUE=$(echo "$OUTPUT" | jq -r '.tracking_issue // empty')
          DETAILS=$(echo "$OUTPUT" | jq -r '.details // empty')
          {
            echo "kind=$KIND"
            echo "phase=$PHASE"
            echo "tracking_issue=$TRACKING_ISSUE"
            echo "details=$DETAILS"
          } >> "$GITHUB_OUTPUT"

      - name: Report kill-switch active
        if: ${{ steps.next.outputs.kind == 'kill-switch' }}
        run: |
          echo "::notice title=Kill switch active::No phase will fire this tick. Details: ${{ steps.next.outputs.details }}"

      - name: Report not-ready state
        if: ${{ steps.next.outputs.kind == 'not-ready' }}
        run: |
          echo "::notice title=Not ready::${{ steps.next.outputs.details }}"

      - name: Fire the next phase
        if: ${{ steps.next.outputs.kind == 'ready' }}
        id: fire
        # Don't fail the job on exit-4 (FireFailure) — we want the
        # marker-commit + digest-comment + Slack steps to all run regardless.
        continue-on-error: true
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          # Codex workflow-R1 [P2] fix: the session needs GH auth to open
          # PRs / push branches / file issues. GITHUB_TOKEN inherits the
          # workflow's permissions block (contents/issues/pull-requests:
          # write); without GH_TOKEN explicit, `gh` and `git push` from the
          # session fail unauthenticated.
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Codex workflow-R2 [P1] + R3 [P1] fix: leave a cleanup budget for
          # the always-run marker/digest steps. The wrapper-internal timeout
          # is computed in shell (GHA expressions don't support arithmetic
          # operators — `${{ (x - 5) * 60 * 1000 }}` is a parse error). Pass
          # the raw minutes; subtract 5 (cleanup buffer) and convert to ms
          # inside the run block.
          TIMEOUT_MINUTES_RAW: ${{ inputs.timeout-minutes }}
        run: |
          set -e
          # Reserve 5-minute cleanup buffer; never go below 60s.
          BUFFER_MIN=5
          INNER_MIN=$(( TIMEOUT_MINUTES_RAW - BUFFER_MIN ))
          if [ "$INNER_MIN" -lt 1 ]; then INNER_MIN=1; fi
          export SESSION_ORCHESTRATOR_TIMEOUT_MS=$(( INNER_MIN * 60 * 1000 ))
          echo "Wrapper timeout: ${INNER_MIN}m (${SESSION_ORCHESTRATOR_TIMEOUT_MS}ms); job timeout: ${TIMEOUT_MINUTES_RAW}m"
          PHASE='${{ steps.next.outputs.phase }}'
          if [ '${{ inputs.dry-run }}' = 'true' ]; then
            session-orchestrator run --phase "$PHASE" --dry-run
          else
            session-orchestrator run --phase "$PHASE"
          fi

      # R3 [P1] acceptance criterion: failure markers must be durable across
      # workflow runs, else a failed phase silently re-fires next tick.
      # We commit ONLY phase-*.{started,failed} (never the runs/ subdir).
      #
      # Codex workflow-R1 fixes baked in:
      #  - [P1] Use `find` for existence-based discovery (literal glob fails
      #    when one of .started or .failed is missing).
      #  - [P1] Commit from an ISOLATED clean clone of the default branch.
      #    The session may have moved HEAD to a phase branch / accumulated
      #    work commits in the workspace; pushing HEAD here would
      #    fast-forward the default branch with unreviewed code (or fail on
      #    a protected branch). The temp clone gives a guaranteed-clean base.
      - name: Commit + push markers (always, even on failure)
        if: ${{ always() && steps.next.outputs.kind == 'ready' && inputs.dry-run == false }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
        run: |
          set -e
          # Collect the marker files that actually exist in the workspace.
          mapfile -d '' -t MARKERS < <(find .session-orchestrator -maxdepth 1 -type f \
            \( -name 'phase-*.started' -o -name 'phase-*.failed' \) -print0)
          if [ "${#MARKERS[@]}" -eq 0 ]; then
            echo "No marker files in workspace; nothing to persist."
            exit 0
          fi
          echo "Markers to persist: ${MARKERS[*]}"

          # Capture marker contents BEFORE switching trees.
          MARKER_BUNDLE=$(mktemp -d)
          mkdir -p "$MARKER_BUNDLE/.session-orchestrator"
          for m in "${MARKERS[@]}"; do
            cp "$m" "$MARKER_BUNDLE/.session-orchestrator/$(basename "$m")"
          done

          # Fresh clone of the default branch into a side dir — guaranteed
          # clean even if the session moved HEAD or accumulated working-tree
          # changes. Shallow clone for speed; force the auth token so push
          # works.
          MARKER_CLONE=$(mktemp -d)
          gh repo clone "${{ github.repository }}" "$MARKER_CLONE" -- \
            --depth 1 --branch "$DEFAULT_BRANCH" --no-tags
          cd "$MARKER_CLONE"
          git config user.name "session-orchestrator[bot]"
          git config user.email "session-orchestrator@noreply.anthropic.com"
          mkdir -p .session-orchestrator
          cp "$MARKER_BUNDLE/.session-orchestrator/"* .session-orchestrator/
          # Stage only matching files (idempotent — `git add <file>` on
          # something already-tracked-and-unchanged is a no-op).
          for m in "${MARKERS[@]}"; do
            git add ".session-orchestrator/$(basename "$m")"
          done
          if git diff --cached --quiet; then
            echo "No marker changes to commit (all files match default branch already)."
          else
            git commit -m "orchestrator: markers for phase ${{ steps.next.outputs.phase }} (run #${{ github.run_id }})"
            git push origin "HEAD:${DEFAULT_BRANCH}"
          fi

      - name: Post digest comment to tracking issue
        if: ${{ always() && steps.next.outputs.kind == 'ready' && steps.next.outputs.tracking_issue != '' && inputs.dry-run == false }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -e
          PHASE='${{ steps.next.outputs.phase }}'
          ISSUE='${{ steps.next.outputs.tracking_issue }}'
          # The wrapper writes the digest at .session-orchestrator/runs/phase-N-<iso>.digest.md.
          DIGEST=$(ls -t .session-orchestrator/runs/phase-${PHASE}-*.digest.md 2>/dev/null | head -1)
          if [ -z "$DIGEST" ]; then
            echo "No digest file from this fire — likely a marker-collision or pre-spawn failure."
            exit 0
          fi
          echo "Posting digest from $DIGEST to issue #${ISSUE}"
          {
            cat "$DIGEST"
            echo ""
            echo "---"
            echo "Run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
            echo "Fire outcome: ${{ steps.fire.outcome }}"
          } > /tmp/digest-with-footer.md
          gh issue comment "$ISSUE" --repo "${{ github.repository }}" --body-file /tmp/digest-with-footer.md

      - name: Optional Slack notification
        # Codex workflow-R3 [P3] fix: step-level `env:` is NOT available in
        # the same step's `if:` expression, so the previous form
        # `if: ... env.SLACK_WEBHOOK_URL != ''` was always false → Slack
        # never fired. Move the secret-presence check into the run block
        # itself and short-circuit there. The step's `if` is now scoped
        # only to "should this notification stage run at all?"
        if: ${{ always() && steps.next.outputs.kind == 'ready' && inputs.dry-run == false }}
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          set -e
          if [ -z "$SLACK_WEBHOOK_URL" ]; then
            echo "SLACK_WEBHOOK_URL not configured; skipping Slack notification."
            exit 0
          fi
          PHASE='${{ steps.next.outputs.phase }}'
          OUTCOME='${{ steps.fire.outcome }}'
          MARK=$([ "$OUTCOME" = "success" ] && echo "OK" || echo "FAILED")
          RUN_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          BODY=$(jq -nc \
            --arg text "session-orchestrator: phase ${PHASE} ${MARK} (${OUTCOME}) — ${RUN_URL}" \
            '{text: $text}')
          curl -fsS -X POST -H 'Content-Type: application/json' -d "$BODY" "$SLACK_WEBHOOK_URL"

      - name: Fail the job if the fire failed
        # Comes after marker-commit + digest + Slack so all observability lands first.
        if: ${{ steps.next.outputs.kind == 'ready' && steps.fire.outcome == 'failure' && inputs.dry-run == false }}
        run: |
          echo "::error title=Phase fire failed::See digest comment on issue #${{ steps.next.outputs.tracking_issue }} for details."
          exit 1
